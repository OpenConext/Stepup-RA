<a{% for attr, value in options %} {{ attr }}="{{ value }}"{% endfor %}>
    {{ title }}
    {% if sorted %}
        <i class="fa fa-sort-{{ options.class }}"></i>
    {% else %}
        <i class="fa fa-sort"></i>
    {% endif %}
</a>
